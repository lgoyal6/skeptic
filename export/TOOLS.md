# lab -- corrected specification

Generated by skeptic on 2026-09-06 05:37 UTC from 4 confirmed beliefs.

The published documentation for this tool is wrong in the ways listed below.
Each entry was established by an experiment, not by assumption: the probe
that settled it is named, and predictions were committed before the
experiment ran.

Read this instead of the vendor docs, or load the accompanying guards and
have them enforced for you.


# eventual consistency
## `search`

**The docs say:** items are immediately searchable

**Actually:** The search query returned zero rows after a 0.5 second wait.

**Do this:** Ensure items are published before searching.

| | |
| --- | --- |
| class | `eventual_consistency` |
| operation | `search` |
| parameter | `-` |
| confidence | P(docs correct) = 0.09 after 1 observations |
| settled by | `probe-009`, `probe-009`, `probe-009`, `probe-009`, `probe-009`, `probe-009`, `probe-009`, `probe-009`, `probe-009`, `probe-009`, `probe-009`, `probe-009`, `probe-009` |
| survived falsification | not yet challenged |


# rate limit
## `create` / `3`

**The docs say:** no rate limit; 429 never returned

**Actually:** The endpoint enforces a rate limit where the first 3 requests succeed and subsequent requests are throttled, and the Retry-After header is unreliable.

**Do this:** Implement exponential backoff instead of relying on the Retry-After header.

| | |
| --- | --- |
| class | `rate_limit` |
| operation | `create` |
| parameter | `3` |
| confidence | P(docs correct) = 0.25 after 1 observations |
| settled by | `probe-001`, `probe-001` |
| survived falsification | not yet challenged |

## `search` / `3`

**The docs say:** no rate limit; 429 never returned

**Actually:** The system throttles 7 out of 10 requests, with the first throttled request occurring at index 4.

**Do this:** Do not exceed 3 requests in a single burst.

| | |
| --- | --- |
| class | `rate_limit` |
| operation | `search` |
| parameter | `3` |
| confidence | P(docs correct) = 0.08 after 1 observations |
| settled by | `probe-010`, `probe-010`, `probe-010`, `probe-010`, `probe-010`, `probe-010`, `probe-010`, `probe-010`, `probe-010`, `probe-010`, `probe-010`, `probe-010`, `probe-006`, `probe-010`, `probe-006` |
| survived falsification | not yet challenged |


# silent coercion
## `create` / `due_date`

**The docs say:** stored as sent

**Actually:** The system accepts '1969-12-31' during creation but returns null on read-back.

**Do this:** Avoid sending '1969-12-31' or handle the null response.

| | |
| --- | --- |
| class | `silent_coercion` |
| operation | `create` |
| parameter | `due_date` |
| confidence | P(docs correct) = 0.25 after 4 observations |
| settled by | `probe-002`, `probe-002` |
| survived falsification | not yet challenged |

---

## What is NOT claimed

Anything absent from this list was either never observed to misbehave or was
observed but never settled by an experiment. Absence here is not a promise
that the docs are right about it.

- unsettled: Cursors expire after a fixed time duration (e.g., 1 hour) to prevent stale data.
- unsettled: Cursors expire if no requests are made for a specific duration (e.g., 30 minutes of inactivity).
- unsettled: The field `not_a_real_field` was present in the readback and stored as null, despite being sent with the value 'test_value'.
