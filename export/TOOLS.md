# lab -- corrected specification

Generated by skeptic on 2026-09-06 00:55 UTC from 5 confirmed beliefs.

The published documentation for this tool is wrong in the ways listed below.
Each entry was established by an experiment, not by assumption: the probe
that settled it is named, and predictions were committed before the
experiment ran.

Read this instead of the vendor docs, or load the accompanying guards and
have them enforced for you.


# eventual consistency
## `search`

**The docs say:** items are immediately searchable

**Actually:** The search query returned zero rows after a 0.5 second wait.

**Do this:** Ensure items are published before searching.

| | |
| --- | --- |
| class | `eventual_consistency` |
| operation | `search` |
| parameter | `-` |
| confidence | P(docs correct) = 0.19 after 1 observations |
| settled by | `probe-009`, `probe-009`, `probe-009` |
| survived falsification | not yet challenged |


# idempotency hazard
## `update` / `not_a_real_field`

**The docs say:** 400 unknown_field

**Actually:** The field `not_a_real_field` was present in the readback and stored as null, despite being sent with the value 'test_value'.

**Do this:** Be cautious with required fields; assume the API might be lenient.

| | |
| --- | --- |
| class | `idempotency_hazard` |
| operation | `update` |
| parameter | `not_a_real_field` |
| confidence | P(docs correct) = 0.19 after 1 observations |
| settled by | `probe-011`, `probe-011`, `probe-011` |
| survived falsification | not yet challenged |


# rate limit
## `search`

**The docs say:** no rate limit; 429 never returned

**Actually:** 7 out of 10 requests were throttled, with the first throttling occurring on the 4th request.

**Do this:** Retry with exponential backoff.

| | |
| --- | --- |
| class | `rate_limit` |
| operation | `search` |
| parameter | `-` |
| confidence | P(docs correct) = 0.19 after 1 observations |
| settled by | `probe-010`, `probe-010`, `probe-010` |
| survived falsification | not yet challenged |


# silent coercion
## `create` / `due_date`

**The docs say:** stored as sent

**Actually:** The due_date field was sent as '1969-12-31' but stored as null.

**Do this:** Ensure dates are within the supported range.

| | |
| --- | --- |
| class | `silent_coercion` |
| operation | `create` |
| parameter | `due_date` |
| confidence | P(docs correct) = 0.24 after 1 observations |
| settled by | `probe-003`, `probe-003` |
| survived falsification | not yet challenged |


# undocumented flag
## `get`

**The docs say:** GET returns the item regardless of archived state

**Actually:** The item was returned successfully with the status 'active', matching the sent value.

**Do this:** Ensure context consistency (API keys, tenants) across operations.

| | |
| --- | --- |
| class | `undocumented_flag` |
| operation | `get` |
| parameter | `-` |
| confidence | P(docs correct) = 0.19 after 1 observations |
| settled by | `probe-004`, `probe-004`, `probe-004` |
| survived falsification | not yet challenged |

---

## What is NOT claimed

Anything absent from this list was either never observed to misbehave or was
observed but never settled by an experiment. Absence here is not a promise
that the docs are right about it.

- unsettled: The API accepts up to 2000 characters, but the response payload is truncated for display purposes while the database stores the full length.
- unsettled: The API enforces a hard internal limit of 255 characters for the 'title' field, overriding the documented 2000 limit.
- unsettled: Cursors expire after a fixed time duration (e.g., 1 hour) to prevent stale data.
- unsettled: Cursors expire if no requests are made for a specific duration (e.g., 30 minutes of inactivity).
- unsettled: The API enforces a hard limit of 50 results per page regardless of the request.
- unsettled: The API sorts by 'created' descending, but the documentation claims ascending.
- unsettled: The API sorts by the string value of the field 'created' (alphabetically), not the timestamp value.
