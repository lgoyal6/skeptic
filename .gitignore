.venv/
__pycache__/
*.pyc
.env
runs/*.jsonl
runs/scratch/
runs/bundles/
!runs/observable.json
!runs/.gitkeep
!probes/.gitkeep
node_modules/
ui/dist/
.DS_Store
ADVERSARY_BRIEF.md
.agent-work/

# Probe records and bench results are EVIDENCE, not build output. The whole
# claim of this project is that its numbers came from experiments a reader can
# inspect, so the experiments ship with it.

# generated by `make evaluate`; the narrative and numbers live in docs/EVALUATION.md
evaluation.json
