  probing 2 rival(s) -> probe-3627958-0
    ? The API has a hard batch limit (e.g., 20 items) that was exceeded, causing the excess to
    ? The transaction failed for the first 10 items (e.g., validation error), causing a rollba
